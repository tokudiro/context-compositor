typst==0.15.0
markdown-it-py==3.0.0
mdit-py-plugins==0.4.0
PyYAML==6.0.2

# plugins.mermaid: true（既定）を使う場合のみ必要（11章）。Mermaidを使わないプロジェクトは
# 不要なので requirements.txt には含めない: pip install playwright==1.62.0
