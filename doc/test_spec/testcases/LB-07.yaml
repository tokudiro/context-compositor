id: LB-07
title: "alignブロックの基本動作（tests/test_layout_blocks.py::TestAlignBlock）"
priority: Low
steps: |
  - `align`属性省略時と、`{align=right}`のように寄せを明示した`::: align ... :::`ブロックを`render()`に渡す。
expected: |
  - 省略時は既定の左寄せ（`#align(left)[...]`）になること
  - `{align=right}`等を指定した場合はその寄せが反映されること
