id: MD-09
title: "非対応HTMLの扱い（tests/test_markdown_basics.py::TestHtmlHandling）"
priority: Medium
steps: |
  - `<div>hi</div>`のような、特別扱い対象（タスクリストのチェックボックス、`<span style="color:...">`）以外のHTMLタグを含むMarkdownを`render()`に渡す。
expected: |
  - HTMLタグは出力から取り除かれ（サイレントに握りつぶさず）、`[Warning]`が出力されること
