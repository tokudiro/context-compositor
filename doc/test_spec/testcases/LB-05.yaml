id: LB-05
title: "layout-columnsの基本動作（tests/test_layout_blocks.py::TestLayoutColumns）"
priority: Medium
steps: |
  - 列数省略時と、`{n=3}`のように列数を明示した`::: layout-columns ... :::`ブロックを`render()`に渡す。
expected: |
  - 省略時は既定の2列で`#columns(...)`が出力されること
  - `{n=3}`を指定した場合は3列で出力されること
